import {
  truncatedString
} from 'messages/helpers/truncated-string';

module('TruncatedStringHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = truncatedString(42);
  ok(result);
});
